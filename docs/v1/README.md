# Docs v1

This folder is the version boundary marker for the v1 document set.

Current v1 materials are still hosted in legacy top-level folders for
backward-compatible references:

- `docs/proof-repair-agent-prd.md`
- `docs/modules/*.md` (except v1.5 upgrade docs)
- `docs/architecture/*.md` (except v1.5 traceability matrix)
- `docs/testing/*.md` (except v1.5 acceptance-gate spec)

Use `docs/v1_5/` for the next-phase design and implementation plan.
